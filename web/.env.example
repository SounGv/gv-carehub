# URL ของ Google Apps Script Web App (Deploy > New deployment > Web app)
# ตัวอย่าง: https://script.google.com/macros/s/XXXXXXXXXXXXXXXX/exec
NEXT_PUBLIC_GV_API_URL=

# TODO(auth): ระบบ Login จริงยังไม่ได้ต่อ ตอนนี้ lib/auth.ts เป็น Auth Adapter ชั่วคราว
# เมื่อมีระบบ Login จริง (เช่น NextAuth / Firebase Auth / Google Workspace SSO) ให้เพิ่ม
# Environment Variable ของผู้ให้บริการนั้นที่นี่ และห้ามเก็บ Secret Key ใด ๆ ไว้ในโค้ด Client
